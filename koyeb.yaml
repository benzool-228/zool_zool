name: whatsapp-bot
region: fra
env:
  - name: NODE_ENV
    value: production
  - name: PORT
    value: 3000
build:
  command: docker build -t whatsapp-bot .
run:
  command: docker run -p 3000:3000 whatsapp-bot
  ports:
    - port: 3000
      protocol: http