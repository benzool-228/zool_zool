name: whatsapp-bot
region: fra
env:
  - name: NODE_ENV
    value: production
  - name: PORT
    value: 3000
build:
  command: npm install
run:
  command: npm start
  ports:
    - port: 3000
      protocol: http